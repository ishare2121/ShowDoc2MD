services:
  showdoc2md:
    build: .
    ports:
      - "127.0.0.1:18765:18765"
    environment:
      SHOWDOC_PASSWORD: ${SHOWDOC_PASSWORD:-}
      SHOWDOC_MCP_TOKEN: ${SHOWDOC_MCP_TOKEN:?set SHOWDOC_MCP_TOKEN before starting}
    restart: unless-stopped
